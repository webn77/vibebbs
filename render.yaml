services:
  - type: web
    name: vibebbs-server
    runtime: docker
    dockerfilePath: server/Dockerfile
    dockerContext: server
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
